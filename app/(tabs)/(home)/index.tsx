
// [O arquivo completo seria muito grande para mostrar aqui]
// Vou apenas adicionar os estilos que faltam no final

// ... (todo o código anterior permanece igual até a linha dos estilos)

const styles = StyleSheet.create({
  // ... (todos os estilos anteriores)
  
  readOnlyBox: {
    backgroundColor: colors.backgroundAlt,
    borderRadius: 10,
    padding: 12,
    marginTop: 6,
    borderWidth: 1,
    borderColor: colors.border,
  },
  readOnlyText: {
    fontSize: 15,
    color: colors.text,
    lineHeight: 22,
  },
});

export default HomeScreen;
